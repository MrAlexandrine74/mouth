%h2 Posts
- if @posts.empty?
  %p Dude! What's a blog without posts? Get to work!
- else
  %ul  
    - @posts.each do |post|
      %li
        = link_to post, admin_post_path(post.id)
        = link_to 'Edit', edit_admin_post_path(post.id)
        = link_to 'Destroy', admin_post_path(post.id), :confirm => 'Are you sure?', :method => :delete

%p= link_to 'New post', new_admin_post_path
